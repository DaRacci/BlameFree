# crb-consensus

Multi-agent consensus orchestration for code review evaluation that runs multiple LLM reviewers concurrently and aggregates their findings via heuristic matching and LLM judge fallback.
