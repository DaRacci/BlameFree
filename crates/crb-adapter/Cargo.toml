[package]
name = "crb-adapter"
version = "0.1.0"
edition = "2021"
description = "Converts harness findings to candidates.json format for the Python step3_judge_comments.py"

[dependencies]
clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
